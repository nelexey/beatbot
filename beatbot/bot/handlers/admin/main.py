from asyncio.locks import _ContextManagerMixin
from aiogram import Router
from typing import Tuple

def register_admin_handlers(dp: Router) -> Tuple[Router, ...]:
    routers = (
    )

    return routers
